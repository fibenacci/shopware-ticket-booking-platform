{
    "name": "fib-seat-picker",
    "private": true,
    "description": "Seat picker as a framework-agnostic custom element (<fib-seat-picker>): Vue 3 inside a shadow root, consumed by the storefront calendar widget — and portable to any future (headless) frontend.",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "test": "vitest run"
    },
    "dependencies": {
        "vue": "latest"
    },
    "devDependencies": {
        "@vitejs/plugin-vue": "latest",
        "vite": "latest",
        "vitest": "latest"
    }
}
